import { UserTwitterFavorites } from './user-twitter-favorites';

describe('UserTwitterFavorites', () => {
  it('should create an instance', () => {
    expect(new UserTwitterFavorites()).toBeTruthy();
  });
});
