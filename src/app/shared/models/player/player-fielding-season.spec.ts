import { PlayerFieldingSeason } from './player-fielding-season';

describe('PlayerFieldingSeason', () => {
  it('should create an instance', () => {
    expect(new PlayerFieldingSeason()).toBeTruthy();
  });
});
