import { UserFavoritePlayer } from './user-favorite-player';

describe('UserFavoritePlayer', () => {
  it('should create an instance', () => {
    expect(new UserFavoritePlayer()).toBeTruthy();
  });
});
