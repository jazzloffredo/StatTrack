import { PlayerBattingSeason } from './player-batting-season';

describe('PlayerBattingSeason', () => {
  it('should create an instance', () => {
    expect(new PlayerBattingSeason()).toBeTruthy();
  });
});
