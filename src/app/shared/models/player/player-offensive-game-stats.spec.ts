import { PlayerOffensiveGameStats } from './player-offensive-game-stats';

describe('PlayerOffensiveGameStats', () => {
  it('should create an instance', () => {
    expect(new PlayerOffensiveGameStats()).toBeTruthy();
  });
});
