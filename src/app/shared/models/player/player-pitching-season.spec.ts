import { PlayerPitchingSeason } from './player-pitching-season';

describe('PlayerPitchingSeason', () => {
  it('should create an instance', () => {
    expect(new PlayerPitchingSeason()).toBeTruthy();
  });
});
